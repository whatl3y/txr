import { ITxrInterface, ITxrInterfaceOpts } from './index';
export default function libraryClientInterface({ socket, socketStream, writeStream, file, user, targetUser, host, logger, callback, reject, resolve, }: ITxrInterfaceOpts): ITxrInterface;
